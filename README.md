# Testing Loopback RESTAPI (SWAPI Model)

Clone this repository, then follow the steps below.

```sh
npm install
npm start
```

Now you're running a virtual server (probably) at ports/urls:
- localhost:3000
- localhost:3000/explorer

:)
